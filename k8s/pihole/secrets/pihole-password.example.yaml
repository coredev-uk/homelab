# PLACEHOLDER: Create your actual secret here
# 
# This is a template. To create the real secret:
#
# 1. Copy this file: cp pihole-password.example.yaml pihole-password.yaml
# 2. Edit with real password: nvim pihole-password.yaml
# 3. Encrypt: sops --encrypt --in-place pihole-password.yaml
# 4. Update kustomization.yaml to include: secrets/pihole-password.yaml
# 5. Commit: git add secrets/ && git commit -m "feat: add pihole password"

apiVersion: v1
kind: Secret
metadata:
  name: pihole-password
  namespace: pihole
type: Opaque
stringData:
  password: "CHANGEME_TO_YOUR_ACTUAL_PASSWORD"
