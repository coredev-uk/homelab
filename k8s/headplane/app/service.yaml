apiVersion: v1
kind: Service
metadata:
  name: headplane
  namespace: headplane
spec:
  type: ClusterIP
  selector:
    app: headplane
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
