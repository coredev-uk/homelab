apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml

configMapGenerator:
  - name: grafana-dashboards
    files:
      - dashboards/cluster-overview.json
      - dashboards/security.json
      - dashboards/media.json
