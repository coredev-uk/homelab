# yaml-language-server: $schema=https://json.schemastore.org/kustomization.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Monitoring configuration - Prometheus Operator is deployed in infrastructure
resources: []
