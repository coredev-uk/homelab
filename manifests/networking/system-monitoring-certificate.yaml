apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: system-monitoring-tls
  namespace: system-monitoring
spec:
  secretName: system-monitoring-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - notifiarr.home.coredev.uk