apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: security-tls
  namespace: security
spec:
  secretName: security-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - frigate.home.coredev.uk