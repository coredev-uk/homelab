apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: security

resources:
  - deployment.yaml

configMapGenerator:
  - name: frigate-config
    files:
      - config.yml=config.yaml